const Loader = () => (
  <div className="flex justify-center">
    <span
      role="img"
      aria-label="Loading"
      className="ease-in-out animate-ping text-6xl"
    >
      ⏳
    </span>
  </div>
);

export default Loader;
